import { ButtonStylesNames, createStyles } from "@mantine/core";

const styles: Record<ButtonStylesNames, {}> = {
  icon: {},
  inner: {},
  label: {},
  leftIcon: {},
  rightIcon: {},
  root: {},
};

export default createStyles(styles);
