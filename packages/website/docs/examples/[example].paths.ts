async function paths() {
  // const meta = await readExampleMeta();
  const paths = [];

  paths.push({
    params: {
      example: "basic",
    },
  });

  return paths;
}

export default { paths };
