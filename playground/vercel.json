{
  "installCommand": "cd .. && corepack enable && pnpm install",
  "buildCommand": "cd .. && pnpm exec vp run --concurrency-limit 1 --filter '@blocknote/example-editor...' build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
