{
  "installCommand": "cd .. && corepack enable && pnpm install",
  "buildCommand": "cd .. && RAYON_NUM_THREADS=4 NODE_OPTIONS=--v8-pool-size=4 pnpm exec vp run --verbose --concurrency-limit 1 --filter '@blocknote/example-editor...' build",
  "outputDirectory": "dist",
  "ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" = \"gh-pages\" ]",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
